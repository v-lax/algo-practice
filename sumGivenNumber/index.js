function sum(input){
    var start = 1;
    let final;
    let sum = 0;

    for(var i=1;i<input;i++){
        sum += (start+i)
    }

    return final = sum + start
}

var answer = sum(10);
console.log(answer)